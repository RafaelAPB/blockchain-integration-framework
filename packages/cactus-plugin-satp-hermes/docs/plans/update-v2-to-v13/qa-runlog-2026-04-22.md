# SATP Hermes QA Runlog 2026-04-22

## Scope

- Package: `@hyperledger/cactus-plugin-satp-hermes`
- Plan reference: `plan/process-satp-hermes-qa-review-1.md`
- Node runtime: `v20.20.0`
- Package manager: Yarn `4.3.1`

## Command Execution Summary

| ID | Command | Exit | Result |
| --- | --- | --- | --- |
| CMD-001 | `yarn run configure` (root) | 1 | Failed due missing pnpm map entry for `@mermaid-js/mermaid-cli` before install. |
| CMD-002 | `yarn install` (root) | 0 | Completed with warnings; dependencies reconciled. |
| CMD-003 | `yarn run configure` (root) | 2 | Failed with TypeScript compilation errors across workspace (185 errors observed in SATP Hermes). |
| CMD-004 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run tsc --noEmit` | 1 | Failed: `185 errors in 65 files`. |
| CMD-005 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run lint` | 0 | Completed (no lint failure emitted; output contains informational duplication checks and formatting status). |
| CMD-006 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run test:unit` | 1 | Failed: `10 failed, 24 passed, 34 total`; 311 tests passed but suites failed at compile stage. |
| CMD-007 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run test:integration:crash-recovery` | 124 (timeout) | Timeout after 300s; repeated Temporal workflow bundle export failures for `rollbackWorkflow` and `crashRecoveryChildWorkflow`. |
| CMD-008 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run test:integration:gateway` | 1 | Failed: `7 failed, 7 total`; same missing export cluster (TokenType, AdminApi, OracleApi, TransactionApi, GetApproveAddressApi, Configuration, HealthCheckResponseStatusEnum, StatusResponseStatusEnum, TransactRequest). |
| CMD-009 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run tsc --noEmit` (post-fix) | **0** | **PASS** — 185 errors eliminated: restored `export *` from generated client in `public-api.ts`; fixed `config.temporalAddress` TS2322 cast in CLI. |
| CMD-010 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run lint` (post-fix) | **0** | **PASS** — lint unchanged; zero new warnings introduced. |
| CMD-011 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run test:unit` (post-fix) | **0** | **PASS** — 34 suites, 439 tests, 0 failures. |
| CMD-012 | per-file crash-recovery (post-fix, `timeout 180s` each) | mixed | `crash-recovery-workflow-signals.test.ts` 2/2 PASS, `rollback-workflow-ponr.test.ts` 3/3 PASS, `rollback-workflow.test.ts` 2/2 PASS, `temporal-crash-recovery-workflow.test.ts` 2/2 PASS. `backup-gateway-promotion.test.ts` and `saga-compensation.test.ts` EXIT:143 (pre-existing hang, see RB-007). |
| CMD-013 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run test:integration:gateway` (post-fix) | 1 | **PARTIAL** — 3/7 suites PASS (`gateway-init-startup`, `gateway-blo`, `SATPGatewayRunner-instantiation`); 4/7 FAIL — all failures caused by Docker API version mismatch: host v1.52 > container max v1.43 (see RB-008). |
| CMD-014 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run test:integration:crash-recovery` (post-RB-007 fix) | **0** | **PASS** — 6/6 suites, 16/16 tests; full crash-recovery integration green. |
| CMD-015 | `yarn workspace @hyperledger/cactus-plugin-satp-hermes run test:unit` (post-RB-007 fix) | **0** | **PASS** — 34/34 suites, 439/439 tests; no regressions from `satp-transfer-workflow.ts` `ABORTED`-checkpoint correction. |

## Key Failure Signatures

### TypeScript/Public API Breakage

- Dominant failure class: missing exports from `public-api` and `main/typescript` entrypoint (`NetworkId`, `TokenType`, Oracle and API model symbols).
- Representative diagnostics:
  - `error TS2305: Module '"../../public-api"' has no exported member 'NetworkId'`
  - `error TS2305: Module '"../../../../main/typescript"' has no exported member 'TransactionApi'`
  - `error TS2339` and `TS2320` in oracle/bridge type wiring.

### Unit Suite Failures

- Final unit summary:
  - `Test Suites: 10 failed, 24 passed, 34 total`
  - `Tests: 311 passed, 311 total`
- Failing suites are compile-gated by missing exports in runtime sources.

### Crash Recovery Integration Failures

- Repeated Temporal runtime errors in `saga-compensation.test.ts`:
  - `TypeError: Failed to initialize workflow of type 'rollbackWorkflow': no such function is exported by the workflow bundle`
  - `TypeError: Failed to initialize workflow of type 'crashRecoveryChildWorkflow': no such function is exported by the workflow bundle`
  - Activity path also reports `ApplicationFailure: Lock assertion rejected`.
- Test command exhibits non-terminating behavior (`RUNS ... saga-compensation.test.ts` loop) under current state.

## Gate Status

| Gate | Status | Notes |
| --- | --- | --- |
| Environment bootstrap (`configure`) | FAIL | Initial dependency map issue fixed via `yarn install`; configure still fails on compile errors. |
| Type safety (`tsc --noEmit`) | **PASS** (CMD-009) | 185 errors → 0 after public-api restoration. |
| Lint | **PASS** (CMD-005/010) | No lint exit failure observed; post-fix unchanged. |
| Unit tests | **PASS** (CMD-011) | 34/34 suites, 439/439 tests all pass after RB-001 unblocked compile. |
| Crash-recovery integration | **PASS** (CMD-014) | 6/6 suites, 16/16 tests; RB-007 closed via async-callback wrapping + `ABORTED` checkpoint fix. |
| Gateway integration | **PARTIAL** (CMD-013) | 3/7 suites PASS; 4/7 blocked by Docker API version mismatch (RB-008). |

## Source Changes Made (2026-04-22 Remediation Session)

| File | Change | Resolves |
| --- | --- | --- |
| `src/main/typescript/public-api.ts` | Restored `export * from "./generated/gateway-client/typescript-axios"`, added ClaimFormat, bridge types, constants, GatewayIdentity, FabricConfigJSON re-exports | RB-001, RB-002 |
| `src/main/typescript/plugin-satp-hermes-gateway-cli.ts` | Cast `config.temporalAddress` to `string \| undefined` to fix TS2322 | RB-001 (last error) |
| `src/main/typescript/temporal/workflows/satp-transfer-workflow.ts` | Added `export { crashRecoveryChildWorkflow }` and `export { rollbackWorkflow }` so bundler exposes them at top level | RB-003 |
| `src/main/typescript/temporal/workflows/crash-recovery-workflow.ts` | Added `export { rollbackWorkflow }` so bundles rooted at this module also expose it | RB-003 |
| `src/main/typescript/temporal/workflows/satp-transfer-workflow.ts` | Removed conditional guard that suppressed the `ABORTED` checkpoint when state was `ROLLBACK_STARTED`; the parent transfer workflow always emits `ABORTED` on the failure path. | RB-007 |
| `src/test/typescript/integration/crash-recovery/saga-compensation.test.ts` | Wrapped `handle.result()` + `handle.query()` calls inside `worker.runUntil(async () => { ... })` so the worker is still polling when queries are dispatched. | RB-007 |
| `src/test/typescript/integration/crash-recovery/backup-gateway-promotion.test.ts` | Wrapped state-query test inside `worker.runUntil(async () => { ... })` (query before signal, all under a running worker). | RB-007 |
| `tools/docker/fabric-all-in-one/Dockerfile_v2.x` | Added two `yq` instructions injecting `DOCKER_API_VERSION=1.43` into the `environment` lists of `peer0.org1.example.com` and `peer0.org2.example.com`, pinning the Fabric peer's Go docker client to API v1.43 so it stays compatible with the bundled `docker:24.0.5-dind` daemon. Requires AIO image rebuild + tag bump to take effect. | RB-008 |

## Immediate Blockers for Next QA Phase

1. ~~**RB-007**~~ ✅ **CLOSED 2026-04-22** — saga-compensation and backup-gateway-promotion suites now PASS after async-callback wrapping and `ABORTED` checkpoint fix.
2. **RB-008** 🟡 **PARTIALLY ADDRESSED 2026-04-22** — Source fix in `tools/docker/fabric-all-in-one/Dockerfile_v2.x` injects `DOCKER_API_VERSION=1.43` into both peer service environments via `yq`, pinning the fabric-peer Go docker client to API v1.43 (compatible with the bundled DinD `docker:24.0.5-dind` daemon). To activate: rebuild the AIO image, republish to GHCR, and bump `FABRIC_25_LTS_AIO_IMAGE_VERSION` in `cactus-test-tooling/src/main/typescript/fabric/fabric-test-ledger-v1.ts`. Verification rerun of `test:integration:gateway` is pending image republish.

