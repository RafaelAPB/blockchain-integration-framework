// define simple express server with several endpoints, in one file
// use simple logger form cacti project, hjarcodewd as debug

//mirror / post : returns what was sent on the body
//simple GET / ok 200 with "ok" body

// webhook/post POST  : this endpoint is called to trigger a webhook (POST); the body has the url to call and the body payload 

// webhook/post/accept POST  : this endpoint is called to trigger a webhook (POST); the body has the url to call and the body payload with  

// 
